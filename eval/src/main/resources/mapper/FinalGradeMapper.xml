<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ees.eval.mapper.FinalGradeMapper">

    <resultMap id="FinalGradeResultMap" type="com.ees.eval.domain.FinalGrade">
        <id property="gradeId"           column="grade_id"/>
        <result property="periodId"      column="period_id"/>
        <result property="empId"         column="emp_id"/>
        <result property="totalScore"    column="total_score"/>
        <result property="finalGradeCode" column="final_grade_code"/>
        <result property="isDeleted"     column="is_deleted"/>
        <result property="version"       column="version"/>
        <result property="createdAt"     column="created_at"/>
        <result property="createdBy"     column="created_by"/>
        <result property="updatedAt"     column="updated_at"/>
        <result property="updatedBy"     column="updated_by"/>
    </resultMap>

    <insert id="insert" parameterType="com.ees.eval.domain.FinalGrade">
        INSERT INTO final_grades_51 (
            period_id, emp_id, total_score, final_grade_code,
            is_deleted, version, created_at, created_by, updated_at, updated_by
        ) VALUES (
            #{periodId}, #{empId}, #{totalScore}, #{finalGradeCode},
            #{isDeleted}, #{version}, #{createdAt}, #{createdBy}, #{updatedAt}, #{updatedBy}
        )
    </insert>

    <select id="findByPeriodIdAndEmpId" resultMap="FinalGradeResultMap">
        SELECT *
        FROM final_grades_51
        WHERE period_id = #{periodId}
          AND emp_id = #{empId}
          AND is_deleted = 'n'
    </select>

    <update id="update" parameterType="com.ees.eval.domain.FinalGrade">
        UPDATE final_grades_51
        SET total_score      = #{totalScore},
            final_grade_code = #{finalGradeCode},
            updated_at       = #{updatedAt},
            updated_by       = #{updatedBy},
            version          = version + 1
        WHERE grade_id = #{gradeId}
          AND version = #{version}
    </update>

    <update id="updateBatch">
        <foreach collection="grades" item="grade" separator=";">
            UPDATE final_grades_51
            SET total_score      = #{grade.totalScore},
                final_grade_code = #{grade.finalGradeCode},
                updated_at       = #{grade.updatedAt},
                updated_by       = #{grade.updatedBy},
                version          = version + 1
            WHERE grade_id = #{grade.gradeId}
              AND version = #{grade.version}
        </foreach>
    </update>

    <select id="findByPeriodId" resultMap="FinalGradeResultMap">
        SELECT *
        FROM final_grades_51
        WHERE period_id = #{periodId}
          AND is_deleted = 'n'
    </select>

    <select id="findByPeriodIdAndDeptId" resultMap="FinalGradeResultMap">
        SELECT fg.*
        FROM final_grades_51 fg
        JOIN employees_51 e ON fg.emp_id = e.emp_id
        WHERE fg.period_id = #{periodId}
          AND e.dept_id = #{deptId}
          AND fg.is_deleted = 'n'
          AND e.is_deleted = 'n'
    </select>

    <delete id="deleteByPeriodId">
        DELETE FROM final_grades_51
        WHERE period_id = #{periodId}
    </delete>

</mapper>
